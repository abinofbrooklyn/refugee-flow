---
phase: 10-route-dashboard-ux-improvements
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/data/IBC_crossingCountByCountry.json
  - src/components/RefugeeRoute_map.tsx
autonomous: false
requirements:
  - UX-FRAMING

must_haves:
  truths:
    - "Each of the 10 updated routes frames its data concentration area on initial load"
    - "English Channel shows Dover Strait, not the wider North Sea"
    - "Americas shows US-Mexico border corridor, Denver to Mexico City"
    - "Sidebar collapse triggers reframe to full-width viewport"
    - "Route switch animates smoothly to new route framing"
  artifacts:
    - path: "src/data/IBC_crossingCountByCountry.json"
      provides: "Visually validated bounds for all 10 routes"
      contains: "bounds"
  key_links:
    - from: "src/components/RefugeeRoute_map.tsx"
      to: "src/data/IBC_crossingCountByCountry.json"
      via: "fitBounds reads tuned bounds values"
      pattern: "fitBounds"
---

<objective>
Visually validate and tune per-route bounds values in the browser, then verify sidebar collapse reframing and route-switch animation work correctly.

Purpose: The initial bounds from research are geographic estimates — they need visual tuning against actual data point rendering in the app to match the user's desired framing for each route.
Output: Tuned bounds values confirmed visually, sidebar and navigation working.
</objective>

<execution_context>
@/Users/abinabraham/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abinabraham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-route-dashboard-ux-improvements/10-CONTEXT.md
@.planning/phases/10-route-dashboard-ux-improvements/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tune per-route bounds values in browser</name>
  <files>src/data/IBC_crossingCountByCountry.json, src/components/RefugeeRoute_map.tsx</files>
  <read_first>src/data/IBC_crossingCountByCountry.json, src/components/RefugeeRoute_map.tsx, .planning/phases/10-route-dashboard-ux-improvements/10-CONTEXT.md</read_first>
  <action>
Start the dev server (`npm run dev`) and navigate to each of the 10 updated routes in the browser. For each route, compare the rendered view against the user's target framing described in CONTEXT.md:

- **Eastern Mediterranean:** Tight on Aegean Sea — Greek islands, Turkish coast. Not the wider basin.
- **Central Mediterranean:** Tunisia to southern Italy/Greece, Algeria coast visible. Libya-Italy sea cluster centered.
- **Western Mediterranean:** Morocco-Spain-Alboran corridor. Strait of Gibraltar centered.
- **English Channel:** Dover Strait tight — London/Oxford to Calais/Bruges/Lille.
- **Western Balkans:** Athens/Istanbul up through Balkans to Vienna.
- **Eastern Land Borders:** Poland-Lithuania-Belarus border corridor down to Hungary/Slovakia.
- **Americas:** US-Mexico border corridor. Denver at top, Mexico City at bottom.
- **Western African:** Senegal/Mauritania/Morocco Atlantic coast. Canary Islands approach centered.
- **Horn of Africa:** Djibouti/Yemen/Gulf of Aden corridor.
- **East & Southern Africa:** DRC/Congo to South Africa/Eswatini, east to Madagascar.

For each route that needs adjustment:
1. Use the browser map controls to find the correct viewport
2. Read the approximate bounds from the map viewport (use map.getBounds() in console)
3. Update the `bounds` array in `src/data/IBC_crossingCountByCountry.json`
4. Refresh and verify

Also check:
- `maxZoom: 7` in navigateToRouteBounds is appropriate (sparse routes like East & Southern Africa shouldn't zoom to street level). If any route needs different maxZoom, add per-route maxZoom logic or adjust the global cap.
- If any route's data points extend well beyond the desired view bounds, the bounds are correctly overriding the data spread (this is expected — English Channel data includes Germany but we want Dover Strait).

If the `navigateToRouteBounds` helper needs adjustments (e.g., per-route padding, different maxZoom per route), update `src/components/RefugeeRoute_map.tsx` accordingly.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
- All 10 updated routes visually match user-specified framing
- `npm run build` succeeds after any bounds adjustments
- No TypeScript errors
  </acceptance_criteria>
  <done>All 10 route bounds values are tuned to match user-specified framing; maxZoom cap is appropriate for all routes; build passes</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all route framing</name>
  <files>src/data/IBC_crossingCountByCountry.json</files>
  <action>
Present the running app to the user for visual verification of all 10 updated routes. Ensure dev server is running before presenting the checkpoint. No code changes — this is a verification gate.
  </action>
  <what-built>Per-route map framing using fitBounds for 10 routes. Each route page now auto-frames to show its data concentration area on load, with sidebar-aware padding and smooth animation on route switch.</what-built>
  <how-to-verify>
    1. Start dev server: `npm run dev`
    2. Navigate to each of these 10 routes and verify the initial map framing shows the data area, not empty geography:
       - /route/eastern-mediterranean (Aegean Sea tight)
       - /route/central-mediterranean (Libya-Italy sea cluster)
       - /route/western-mediterranean (Strait of Gibraltar)
       - /route/english-channel (Dover Strait)
       - /route/western-balkans (Athens to Vienna)
       - /route/eastern-land-borders (Poland-Belarus corridor)
       - /route/americas (US-Mexico border)
       - /route/western-african (Canary Islands approach)
       - /route/horn-of-africa (Djibouti/Yemen corridor)
       - /route/east-and-southern-africa (DRC to S. Africa)
    3. Verify unchanged routes still work:
       - /route/iran-afghanistan-corridor (same as before)
       - /route/south-and-east-asia (same as before)
    4. On any route, collapse the sidebar — map should reframe to use full width
    5. Switch between two routes — map should fly/animate smoothly to the new framing
    6. Verify data points (colored circles) render correctly on all routes
  </how-to-verify>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
- User approves framing for all 10 routes
- Sidebar collapse/expand triggers correct viewport reframe
- Route switch animation is smooth
  </acceptance_criteria>
  <done>User has approved the visual framing for all 10 routes; sidebar and route-switch behavior confirmed working</done>
  <resume-signal>Type "approved" or describe which routes need framing adjustments</resume-signal>
</task>

</tasks>

<verification>
- Visual framing matches user target for all 10 updated routes
- Sidebar collapse and expand trigger correct reframing
- Route-to-route navigation animates smoothly
- Data points render on all routes without gaps or rendering artifacts
- Iran-Afghanistan Corridor and South & East Asia unchanged
</verification>

<success_criteria>
- User approves the visual framing for all 10 routes
- Sidebar-aware viewport centering works correctly
- Route switch animation is smooth with no flash or jump
- No visual regressions on unchanged routes
</success_criteria>

<output>
After completion, create `.planning/phases/10-route-dashboard-ux-improvements/10-02-SUMMARY.md`
</output>
