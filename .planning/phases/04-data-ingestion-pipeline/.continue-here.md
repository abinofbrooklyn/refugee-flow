---
phase: 04-data-ingestion-pipeline
task: complete
total_tasks: 16
status: blocked
last_updated: 2026-03-21T07:07:49.619Z
---

<current_state>
Phase 4 is 15/16 plans complete. Plan 07 (ACLED normalization) is blocked on API access. Phase 6 (React Router v6) is fully complete. Data validation layer is built and operational. All 12 route geographic bounds are finalized. User wants to start v2 work (TypeScript migration) while waiting for ACLED.
</current_state>

<completed_work>

### This Session (2026-03-20 to 2026-03-21)

**Phase 6 — React Router v6 Migration (COMPLETE)**
- Plans 06-01 through 06-03 executed: v4→v6 upgrade, withRouter6 HOC, layout routes
- Navbar highlight fix for SPA navigation
- Annotation show-once logic
- Zero legacy context warnings, all routes working

**Phase 4 — Validation Layer (Plans 15-16 COMPLETE)**
- Migration 004: data_quarantine table + ingestion_log quarantine_count column
- validator.js: 3 rule types (geo-label mismatch, outlier coordinates, value anomalies)
- Identical lat/lng detection for fat-fingered IOM data
- UNRESOLVED sentinel for unresolvable coordinates → quarantine
- sendQuarantineAlert: email with every flagged row and details
- Integrated validator into all 7 ingestion pipelines
- 31 validator tests + 4 alerter tests

**Infrastructure Improvements**
- CBP + UK Channel moved from standalone scripts to server-side node-cron
- Admin page removed (security surface eliminated)
- Retry logic (3 attempts, exponential backoff) on all 7 pipelines
- Email alerts via Resend on pipeline failure
- Health endpoint: GET /data/ingestion-health
- ACLED cron disabled until credentials configured
- 4 pre-existing test suites fixed (UNHCR, ACLED, seed, endpoints)

**Data Quality (Iterative Bounds Fixes)**
- ROUTE_MAP: Türkiye-Europe land route → Eastern Mediterranean
- Tightened geographic bounds on all 12 routes using Frontex/IOM authoritative definitions
- Fixed geoFallback for: Caucasus/Russia, Black Sea Turkey, Persian Gulf, Yemen, Central Asia, equatorial Africa, Arabian Sea
- Central Med eastern bound: lng 37 → 21 (excludes Greece/Turkey/Sinai)
- Western Med eastern bound: lng 15 → 5 (Tunisia/Libya → Central Med)
- Horn of Africa western bound: lng 15 → 30
- Full IOM re-ingest: 20,042 clean rows, 11 quarantined
- Route overlap documentation added to all 12 route descriptions

</completed_work>

<remaining_work>

**Phase 4 Plan 07 — BLOCKED**
- ACLED war/conflict data normalization
- Waiting on API access (email sent to access@acleddata.com)
- When access granted: uncomment ACLED_EMAIL/ACLED_PASSWORD in .env, cron auto-enables
- Also: remove JSON seed files (25 MB) after ACLED is working (see memory: project_cleanup_after_acled.md)

**Phase 5 — Data Coverage (NOT STARTED)**
- Data gap audit
- Year range slider validation
- Route coverage verification
- Depends on Phase 4 completion

**v2 — TypeScript Migration (USER WANTS TO START)**
- Convert all .jsx → .tsx
- Add types to components, Redux store, API responses, ingestion modules
- Then: React Testing Library component tests in TypeScript

</remaining_work>

<decisions_made>

- Quarantine + alert pattern for bad data (not reject, not block entire batch)
- Full IOM re-ingest replaces retroactive cleanup migration
- Admin page removed — automation over admin UIs (product philosophy)
- Route bounds derived from Frontex/IOM authoritative country definitions (no published coord boxes exist)
- TypeScript first, then RTL tests (avoid writing JS tests then converting)
- Alpine route (Italy to France) — only 1 record, not worth a 13th route
- IOM route classification kept as source truth — direction of travel, not death location
- Overlap notes added to route descriptions instead of reclassifying data

</decisions_made>

<blockers>
- ACLED API access: email sent to access@acleddata.com — no response yet
- Phase 5 depends on Phase 4 Plan 07 completion
</blockers>

<context>
The app is in strong shape — 7 automated pipelines, validated data, 217 tests, zero console warnings. The main gap is ACLED war/conflict data (still 2010-2018 static data on the globe). User is ready to move to v2 work rather than wait.
</context>

<next_action>
Start v2 TypeScript migration: either /gsd:new-milestone for v2 or /gsd:discuss-phase for a TypeScript phase added to v1 roadmap.
</next_action>
