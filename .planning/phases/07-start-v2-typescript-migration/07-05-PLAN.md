---
phase: 07-start-v2-typescript-migration
plan: 05
type: execute
wave: 2
depends_on: [07-02, 07-03]
files_modified:
  - src/components/RefugeeRoute.tsx
  - src/components/RefugeeRoute_map.tsx
  - src/components/RefugeeRoute_map_popup.tsx
  - src/components/RefugeeRoute_textArea.tsx
  - src/components/RefugeeRoute_textArea_contentManager.tsx
  - src/components/RefugeeRoute_textArea_content_basicInfo.tsx
  - src/components/RefugeeRoute_textArea_content_currentSelectedPoint.tsx
  - src/components/RefugeeRoute_textArea_content_ibcCountry.tsx
  - src/components/RefugeeRoute_textArea_content_ibcCountryItem.tsx
  - src/components/RefugeeRoute_titleGroup.tsx
autonomous: true
requirements: [MOD-V2-01]

must_haves:
  truths:
    - "All 10 RefugeeRoute components converted from class to functional TSX"
    - "MapLibre GL map integration still works (imperative DOM ref pattern preserved)"
    - "Route data fetching uses typed API functions from api.ts"
    - "No connect() HOC or withRouter6 wrapper remains"
    - "tsc --noEmit and Vite build pass"
  artifacts:
    - path: "src/components/RefugeeRoute.tsx"
      provides: "Main route page component"
      contains: "React.FC"
    - path: "src/components/RefugeeRoute_map.tsx"
      provides: "MapLibre GL route map"
      contains: "useRef"
    - path: "src/components/RefugeeRoute_textArea_content_basicInfo.tsx"
      provides: "Route basic info panel (largest file ~747 lines)"
      contains: "React.FC"
  key_links:
    - from: "src/components/RefugeeRoute.tsx"
      to: "src/utils/api.ts"
      via: "Typed API fetch calls"
      pattern: "get_routeDeath|get_routeIBC"
    - from: "src/components/RefugeeRoute_map.tsx"
      to: "maplibre-gl"
      via: "useRef<maplibregl.Map> for imperative map control"
      pattern: "useRef.*Map"
---

<objective>
Convert all RefugeeRoute components from JSX class components to TSX functional components with hooks.

Purpose: The RefugeeRoute family is the second-largest component group (10 files, including the 747-line basicInfo component). Converting these establishes the pattern for map components with imperative DOM refs (MapLibre GL).
Output: 10 JSX files converted to TSX functional components.
</objective>

<execution_context>
@/Users/abinabraham/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abinabraham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-start-v2-typescript-migration/07-CONTEXT.md
@.planning/phases/07-start-v2-typescript-migration/07-RESEARCH.md
@.planning/phases/07-start-v2-typescript-migration/07-UI-SPEC.md
@.planning/phases/07-start-v2-typescript-migration/07-02-SUMMARY.md
@.planning/phases/07-start-v2-typescript-migration/07-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert RefugeeRoute core and map components to TSX</name>
  <files>src/components/RefugeeRoute.tsx, src/components/RefugeeRoute_map.tsx, src/components/RefugeeRoute_map_popup.tsx, src/components/RefugeeRoute_titleGroup.tsx</files>
  <read_first>
    - src/components/RefugeeRoute.jsx (main route page — data fetching, loading/error states)
    - src/components/RefugeeRoute_map.jsx (MapLibre GL integration with imperative ref)
    - src/components/RefugeeRoute_map_popup.jsx (map popup component)
    - src/components/RefugeeRoute_titleGroup.jsx (route header with navigation arrows)
    - src/utils/api.ts (typed API functions)
    - src/types/api.ts (response types)
    - src/data/routeDictionary.ts (route metadata)
  </read_first>
  <action>
For each of the 4 components, apply the standard conversion pattern:

**RefugeeRoute.jsx → RefugeeRoute.tsx:**
- Convert class to functional component: `const RefugeeRoute: React.FC = () => {`
- State variables (loading, error, data) → useState hooks with types: `const [loading, setLoading] = useState<boolean>(true);`
- `componentDidMount` data fetching → `useEffect` with fetch calls to typed API functions
- Error state: preserve the exact error message text "Error loading data. Please try refreshing the page." per UI-SPEC
- Loading state: preserve ScaleLoader spinner
- If uses `connect()`, replace with useAppSelector/useAppDispatch from `../../types/redux`
- If uses `withRouter6`, replace with `useParams` from react-router-dom for route arg: `const { arg } = useParams<{ arg: string }>();`
- Type the component's data state using imported types from `../../types/api`

**RefugeeRoute_map.jsx → RefugeeRoute_map.tsx:**
- This component creates a MapLibre GL map instance imperatively
- Convert `this.map = new maplibregl.Map(...)` to `const mapRef = useRef<maplibregl.Map | null>(null);`
- `componentDidMount` → `useEffect(() => { mapRef.current = new maplibregl.Map({...}); return () => mapRef.current?.remove(); }, []);`
- Type the container div ref: `const containerRef = useRef<HTMLDivElement>(null);`
- Define props interface with all data the map receives from parent

**RefugeeRoute_map_popup.jsx → RefugeeRoute_map_popup.tsx:**
- Simpler component — convert to functional with typed props
- Define popup data interface

**RefugeeRoute_titleGroup.jsx → RefugeeRoute_titleGroup.tsx:**
- Convert to functional component
- If uses withRouter6 or connect(), replace with hooks
- Type the route navigation arrows callbacks
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/components/RefugeeRoute.jsx does NOT exist
    - src/components/RefugeeRoute.tsx contains "React.FC" or "const RefugeeRoute"
    - src/components/RefugeeRoute.tsx does NOT contain "extends React.Component"
    - src/components/RefugeeRoute_map.jsx does NOT exist
    - src/components/RefugeeRoute_map.tsx contains "useRef"
    - src/components/RefugeeRoute_map.tsx does NOT contain "this.map"
    - src/components/RefugeeRoute_map_popup.jsx does NOT exist
    - src/components/RefugeeRoute_titleGroup.jsx does NOT exist
    - None of the 4 converted files contain "connect(" or "withRouter6("
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>RefugeeRoute core and map components converted to typed TSX with MapLibre useRef pattern, typed API calls, no class components remain</done>
</task>

<task type="auto">
  <name>Task 2: Convert RefugeeRoute text area and content components to TSX</name>
  <files>src/components/RefugeeRoute_textArea.tsx, src/components/RefugeeRoute_textArea_contentManager.tsx, src/components/RefugeeRoute_textArea_content_basicInfo.tsx, src/components/RefugeeRoute_textArea_content_currentSelectedPoint.tsx, src/components/RefugeeRoute_textArea_content_ibcCountry.tsx, src/components/RefugeeRoute_textArea_content_ibcCountryItem.tsx</files>
  <read_first>
    - src/components/RefugeeRoute_textArea.jsx
    - src/components/RefugeeRoute_textArea_contentManager.jsx
    - src/components/RefugeeRoute_textArea_content_basicInfo.jsx (largest file ~747 lines)
    - src/components/RefugeeRoute_textArea_content_currentSelectedPoint.jsx
    - src/components/RefugeeRoute_textArea_content_ibcCountry.jsx
    - src/components/RefugeeRoute_textArea_content_ibcCountryItem.jsx
    - src/types/api.ts (for RouteDeath, IbcCrossing types)
    - src/data/routeDictionary.ts (typed route metadata)
  </read_first>
  <action>
For each of the 6 components, apply the standard conversion pattern:

**RefugeeRoute_textArea_content_basicInfo.jsx → basicInfo.tsx (largest at ~747 lines):**
- Read carefully first — this is the most complex non-globe component
- Convert class to functional
- All `this.state` → useState hooks
- All `this.props` → typed props interface (define with all fields)
- If uses `connect()` → useAppSelector/useAppDispatch
- If uses D3 for charts → type the d3 scale/axis/selection calls using @types/d3
- If uses MapLibre for secondary map → useRef<maplibregl.Map> pattern
- Type all data variables using RouteDeath, IbcCrossing from types/api.ts
- **IMPORTANT**: Do not change any styled-component values, inline styles, or rendering logic. This is a pure code-level conversion.

**All other textArea components:**
- Follow the same class → functional pattern
- Define typed props interfaces for data flowing from parent → child
- ibcCountry and ibcCountryItem receive crossing data — type with IbcCrossing interface
- currentSelectedPoint shows clicked map point details — type the point data
- contentManager routes between content views — type the tab/view state

After all 6 are converted:
- Run `npx tsc --noEmit` for type verification
- Run `npm run build` for Vite bundle verification
- Run `npm test` to check no regressions
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build && npm test</automated>
  </verify>
  <acceptance_criteria>
    - All 6 .jsx files do NOT exist (deleted)
    - All 6 .tsx files exist
    - None contain "extends React.Component" or "class "
    - None contain "this.state" or "this.setState" or "this.props"
    - None contain "connect(" or "mapStateToProps"
    - src/components/RefugeeRoute_textArea_content_basicInfo.tsx exists (converted from 747-line source)
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0
    - `npm test` exits 0
  </acceptance_criteria>
  <done>All 6 RefugeeRoute text area components converted to typed TSX functional components, including the 747-line basicInfo component. Build and tests pass.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — zero errors
- `npm run build` — Vite build succeeds
- `npm test` — all existing tests pass
- No .jsx files remain for any RefugeeRoute component (all 10 converted)
- Zero instances of "extends React.Component" in converted files
- Zero instances of "connect(" in converted files
</verification>

<success_criteria>
All 10 RefugeeRoute components fully converted to TSX functional components. MapLibre GL integration uses typed useRef. API calls use typed functions from api.ts. No class components, no connect() HOCs. Build passes.
</success_criteria>

<output>
After completion, create `.planning/phases/07-start-v2-typescript-migration/07-05-SUMMARY.md`
</output>
