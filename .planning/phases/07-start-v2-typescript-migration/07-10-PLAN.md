---
phase: 07-start-v2-typescript-migration
plan: 10
type: execute
wave: 4
depends_on: [07-07, 07-08, 07-09]
files_modified:
  - tests/server/alerter.test.ts
  - tests/server/asylumNormalizer.test.ts
  - tests/server/cbpIngestion.test.ts
  - tests/server/db-connection.test.ts
  - tests/server/endpoints.test.ts
  - tests/server/ingestion-acled.test.ts
  - tests/server/ingestion-eurostat.test.ts
  - tests/server/ingestion-iom.test.ts
  - tests/server/ingestion-unhcr.test.ts
  - tests/server/ingestionHealth.test.ts
  - tests/server/iomNormalizer.test.ts
  - tests/server/rateLimit.test.ts
  - tests/server/retryRunner.test.ts
  - tests/server/seed.test.ts
  - tests/server/ukChannelIngestion.test.ts
  - tests/server/updateCBP.test.ts
  - tests/server/validator.test.ts
autonomous: false
requirements: [MOD-V2-01]

must_haves:
  truths:
    - "All 17 test files converted from .test.js to .test.ts"
    - "Test imports use TypeScript import syntax"
    - "All 17 test suites pass with ts-jest transform"
    - "tsc --noEmit passes for both frontend and server"
    - "Vite build succeeds"
    - "Zero .js/.jsx files remain in src/ or server/ (migration complete)"
    - "Manual smoke test confirms app works end-to-end"
  artifacts:
    - path: "tests/server/endpoints.test.ts"
      provides: "Typed API endpoint integration tests"
      contains: "import"
    - path: "tests/server/validator.test.ts"
      provides: "Typed validator unit tests"
      contains: "import"
  key_links:
    - from: "tests/server/endpoints.test.ts"
      to: "server/server.ts"
      via: "supertest import of Express app"
      pattern: "import.*server"
    - from: "tests/server/ingestion-acled.test.ts"
      to: "server/ingestion/acledIngestion.ts"
      via: "import of ingestion function"
      pattern: "import.*acledIngestion"
---

<objective>
Convert all 17 test files from JavaScript to TypeScript and perform final verification of the complete migration.

Purpose: Test files are the last JavaScript in the project. Converting them completes the full-stack TypeScript migration. A manual smoke test checkpoint confirms the app works end-to-end.
Output: 17 test files converted to .test.ts, final smoke test verification.
</objective>

<execution_context>
@/Users/abinabraham/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abinabraham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-start-v2-typescript-migration/07-CONTEXT.md
@.planning/phases/07-start-v2-typescript-migration/07-RESEARCH.md
@.planning/phases/07-start-v2-typescript-migration/07-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert all 17 server test files from .test.js to .test.ts</name>
  <files>tests/server/alerter.test.ts, tests/server/asylumNormalizer.test.ts, tests/server/cbpIngestion.test.ts, tests/server/db-connection.test.ts, tests/server/endpoints.test.ts, tests/server/ingestion-acled.test.ts, tests/server/ingestion-eurostat.test.ts, tests/server/ingestion-iom.test.ts, tests/server/ingestion-unhcr.test.ts, tests/server/ingestionHealth.test.ts, tests/server/iomNormalizer.test.ts, tests/server/rateLimit.test.ts, tests/server/retryRunner.test.ts, tests/server/seed.test.ts, tests/server/ukChannelIngestion.test.ts, tests/server/updateCBP.test.ts, tests/server/validator.test.ts</files>
  <read_first>
    - tests/server/endpoints.test.js (representative test — understand require patterns)
    - tests/server/validator.test.js (unit test pattern)
    - tests/server/ingestion-acled.test.js (mock pattern)
    - jest.config.js (confirm ts-jest transform is configured for server project)
    - tsconfig.server.json (confirm test files are included)
  </read_first>
  <action>
For each of the 17 test files:

1. Read the current .test.js file
2. Delete the .test.js file
3. Create the .test.ts file with these transformations:
   - `const X = require('Y')` -> `import X from 'Y'` or `import { X } from 'Y'`
   - `const request = require('supertest')` -> `import request from 'supertest'`
   - `const app = require('../../server/server')` -> `import app from '../../server/server'`
   - Type mock functions: `jest.fn()` -> `jest.fn<ReturnType, [ParamTypes]>()` where clear
   - Type test data with interfaces from server/types/knex.ts where applicable
   - For `jest.mock()` calls, the mock factory may need type assertions. Use `as jest.Mock` pattern for mock return values:
     ```typescript
     jest.mock('../../server/database/connection');
     const mockDb = db as jest.Mocked<typeof db>;
     ```
   - For complex mock chains (knex query builder), use `@ts-expect-error` sparingly with explanation when Jest mock types conflict with Knex types

**Important patterns:**
- Server tests use `require()` to import source modules. With `module: "CommonJS"` in tsconfig.server.json, ts-jest will transpile `import` to `require()` — so `import` syntax works correctly.
- `afterAll(() => db.destroy())` pattern must be preserved to prevent open handle warnings
- Some tests may import from paths like `../../server/ingestion/X` — these now resolve to .ts files via ts-jest

**After all 17 files are converted:**
- Update tsconfig.server.json to include test files if not already:
  ```json
  "include": ["server/**/*", "tests/server/**/*"]
  ```
- Run `npm test` to verify all 17 suites pass
- Run `npx tsc --noEmit --project tsconfig.server.json` to verify test file types
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit && npx tsc --noEmit --project tsconfig.server.json</automated>
  </verify>
  <acceptance_criteria>
    - All 17 .test.js files do NOT exist (deleted)
    - All 17 .test.ts files exist
    - None contain "require(" (converted to import)
    - tests/server/endpoints.test.ts contains "import" (not require)
    - tests/server/validator.test.ts contains "import" (not require)
    - tsconfig.server.json include array contains "tests/server/**/*"
    - `npm test` exits 0 with all 17 suites passing
    - `npx tsc --noEmit` exits 0
    - `npx tsc --noEmit --project tsconfig.server.json` exits 0
    - Zero .js files remain in tests/server/
  </acceptance_criteria>
  <done>All 17 test files converted to TypeScript. All suites pass with ts-jest. Type checking passes for both frontend and server configs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Final smoke test — verify complete TypeScript migration</name>
  <action>
All automated conversion is complete. This checkpoint verifies the full migration has no visual or functional regressions by running the formalized smoke test checklist from 07-UI-SPEC.md. The executor should run the build verification commands and present the smoke test checklist to the user.
  </action>
  <what-built>
Complete TypeScript migration of the entire codebase:
- 39 JSX components converted to TSX functional components with hooks
- 12 src JS utility/data files converted to TS
- 21 server JS files converted to TS
- 17 test files converted to .test.ts
- Redux modernized: connect() to useAppSelector/useAppDispatch
- GlobeVisual uses forwardRef + useImperativeHandle
- strict: true with zero any in Redux types
  </what-built>
  <how-to-verify>
Run the formalized smoke test checklist from 07-UI-SPEC.md:

**Build verification:**
1. `npx tsc --noEmit` — should exit 0 (zero frontend TS errors)
2. `npx tsc --noEmit --project tsconfig.server.json` — should exit 0 (zero server TS errors)
3. `npm run build` — should produce dist/ (Vite build)
4. `npm test` — all 17 test suites pass

**Manual smoke test (start dev servers):**
1. Terminal 1: `npm run nodemon` (server on :2700)
2. Terminal 2: `npm start` (Vite on :8080)

**Landing:**
- [ ] Desktop landing page renders
- [ ] Mobile landing page renders (resize browser)

**Globe:**
- [ ] Globe renders with 3D earth texture
- [ ] Conflict data points appear
- [ ] Globe rotation toggle works
- [ ] Year timeline click transitions globe

**Routes:**
- [ ] Navigate to 3+ routes from globe
- [ ] Route map loads with dark basemap
- [ ] IBC chart renders

**About:**
- [ ] About page accordion works

**Navigation:**
- [ ] All nav links work
- [ ] Browser back/forward works

**Admin:**
- [ ] /admin shows login
- [ ] Correct secret grants access

**API:**
- [ ] /data/reduced_war_data returns JSON
- [ ] /data/route_deaths returns JSON
- [ ] /data/asy_applications returns JSON

**File verification:**
- [ ] `find src -name "*.js" -o -name "*.jsx" | grep -v node_modules` returns empty (zero JS/JSX in src/)
- [ ] `find server -name "*.js" | grep -v node_modules` returns empty (zero JS in server/)
- [ ] `find tests -name "*.js" | grep -v node_modules` returns empty (zero JS in tests/)
  </how-to-verify>
  <verify>
    <automated>npx tsc --noEmit && npx tsc --noEmit --project tsconfig.server.json && npm run build && npm test</automated>
  </verify>
  <acceptance_criteria>
    - All smoke test checklist items pass
    - Zero .js/.jsx files in src/, server/, tests/
    - tsc --noEmit exits 0 for both configs
    - npm run build exits 0
    - npm test exits 0
  </acceptance_criteria>
  <done>Complete TypeScript migration verified — all automated checks pass and manual smoke test confirms no regressions</done>
  <resume-signal>Type "approved" if all smoke test items pass, or describe any regressions</resume-signal>
</task>

</tasks>

<verification>
- `npm test` — all 17 suites pass
- `npx tsc --noEmit` — zero frontend errors
- `npx tsc --noEmit --project tsconfig.server.json` — zero server errors
- `npm run build` — Vite produces dist/
- Zero .js/.jsx files remain in src/, server/, tests/ (excluding config files like jest.config.js, enzyme.config.js, vite.config.ts)
- Manual smoke test confirms app works end-to-end
</verification>

<success_criteria>
The entire codebase is TypeScript with strict: true. All 17 test files are .test.ts. Zero JavaScript source files remain. The app builds, all tests pass, and the manual smoke test confirms no visual or functional regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/07-start-v2-typescript-migration/07-10-SUMMARY.md`
</output>
