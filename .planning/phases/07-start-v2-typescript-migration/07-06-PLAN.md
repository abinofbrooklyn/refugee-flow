---
phase: 07-start-v2-typescript-migration
plan: 06
type: execute
wave: 2
depends_on: [07-02, 07-03]
files_modified:
  - src/components/asylumApplication/AsyApplicationChart.tsx
  - src/components/asylumApplication/AsyApplicationChartContainer.tsx
  - src/components/asylumApplication/AsyApplicationContainer.tsx
  - src/components/Conflict.tsx
  - src/components/RegionModalButton.tsx
  - src/components/RegionModalContent.tsx
  - src/components/RegionModalCreator.tsx
  - src/components/RegionModalNav.tsx
  - src/index.tsx
autonomous: true
requirements: [MOD-V2-01]

must_haves:
  truths:
    - "All 9 components/entry points converted from JSX to TSX"
    - "Asylum chart D3 rendering uses typed scale and axis functions"
    - "Conflict page connects to typed Redux store via useAppSelector"
    - "Entry point (index.tsx) renders Provider with typed store"
    - "tsc --noEmit and Vite build pass"
    - "React Test Renderer snapshots captured before conversion; diffs confirm no rendering regressions"
  artifacts:
    - path: "src/index.tsx"
      provides: "App entry point with typed store"
      contains: "createRoot"
    - path: "src/components/Conflict.tsx"
      provides: "Conflict page with hooks"
      contains: "useAppSelector"
    - path: "src/components/asylumApplication/AsyApplicationChart.tsx"
      provides: "D3 chart with typed data"
      contains: "React.FC"
  key_links:
    - from: "src/index.tsx"
      to: "src/redux/store.ts"
      via: "Typed store import for Provider"
      pattern: "import store from.*redux/store"
    - from: "src/components/Conflict.tsx"
      to: "src/types/redux.ts"
      via: "useAppSelector for state access"
      pattern: "useAppSelector"
---

<objective>
Convert asylum application components, Conflict page, RegionModal components, and the app entry point to TSX.

Purpose: Completes the remaining non-globe components. After this plan, only the globe component family remains as unconverted JSX.
Output: 9 files converted from .jsx to .tsx.
</objective>

<execution_context>
@/Users/abinabraham/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abinabraham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-start-v2-typescript-migration/07-CONTEXT.md
@.planning/phases/07-start-v2-typescript-migration/07-RESEARCH.md
@.planning/phases/07-start-v2-typescript-migration/07-UI-SPEC.md
@.planning/phases/07-start-v2-typescript-migration/07-02-SUMMARY.md
@.planning/phases/07-start-v2-typescript-migration/07-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert asylum application and region modal components to TSX</name>
  <files>src/components/asylumApplication/AsyApplicationChart.tsx, src/components/asylumApplication/AsyApplicationChartContainer.tsx, src/components/asylumApplication/AsyApplicationContainer.tsx, src/components/RegionModalButton.tsx, src/components/RegionModalContent.tsx, src/components/RegionModalCreator.tsx, src/components/RegionModalNav.tsx</files>
  <read_first>
    - src/components/asylumApplication/AsyApplicationChart.jsx (D3 chart rendering)
    - src/components/asylumApplication/AsyApplicationChartContainer.jsx (chart container)
    - src/components/asylumApplication/AsyApplicationContainer.jsx (page container)
    - src/components/RegionModalButton.jsx
    - src/components/RegionModalContent.jsx
    - src/components/RegionModalCreator.jsx
    - src/components/RegionModalNav.jsx
    - src/types/api.ts (AsyApplication type for chart data)
    - src/types/redux.ts (useAppSelector/useAppDispatch)
  </read_first>
  <action>
**Pre-conversion snapshot step (per locked regression testing decision):**
Before converting, capture React Test Renderer snapshots:
1. Create `tests/client/snapshots-06-pre.test.tsx`
2. Render each component with appropriate wrappers (Provider, MemoryRouter as needed) and mocked props
3. For AsyApplicationChart, mock the canvas context since jsdom doesn't support it
4. Run to generate baseline snapshots

**Asylum Application components:**

1. **AsyApplicationChart.jsx -> AsyApplicationChart.tsx:**
   - Convert class to functional
   - This component uses D3 for canvas chart rendering
   - Type the D3 scale functions: `d3.scaleLinear<number, number>()`, `d3.scaleTime<number, number>()`
   - Type the chart container ref: `const canvasRef = useRef<HTMLCanvasElement>(null);`
   - `componentDidMount`/`componentDidUpdate` -> useEffect with D3 drawing logic
   - Define props interface with chart data typed as `AsyApplication[]` from types/api.ts
   - For d3-canvas-transition interactions, use `@ts-expect-error` if the library lacks types (it's a small niche library)

2. **AsyApplicationChartContainer.jsx -> AsyApplicationChartContainer.tsx:**
   - Convert class to functional
   - If uses `connect()` -> useAppSelector/useAppDispatch
   - Type the data fetching with API response types

3. **AsyApplicationContainer.jsx -> AsyApplicationContainer.tsx:**
   - Convert class to functional
   - If uses `connect()` -> useAppSelector/useAppDispatch

**RegionModal components:**

4. **RegionModalButton/Content/Creator/Nav.jsx -> .tsx:**
   - Convert each class to functional
   - Type props interfaces (modal data, callbacks)
   - RegionModalCreator likely uses react-modal — type the modal props
   - Import `Modal` from 'react-modal' (has @types/react-modal or built-in types)

**Post-conversion snapshot verification:** Re-run snapshot tests, diff against baseline to confirm zero rendering regressions.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - All 7 .jsx files do NOT exist (deleted)
    - All 7 .tsx files exist
    - None contain "extends React.Component" or "class "
    - None contain "connect(" or "mapStateToProps"
    - src/components/asylumApplication/AsyApplicationChart.tsx contains "useRef" (for canvas)
    - React Test Renderer snapshots confirm no rendering regressions
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>7 components converted to typed TSX functional components with D3 typed scales, snapshot regression verified, no class components or connect() remain</done>
</task>

<task type="auto">
  <name>Task 2: Convert Conflict page and app entry point to TSX</name>
  <files>src/components/Conflict.tsx, src/index.tsx</files>
  <read_first>
    - src/components/Conflict.jsx (conflict page — orchestrates globe, timeline, stats)
    - src/index.jsx (app entry point — Provider + Router)
    - src/redux/store.ts (typed store for Provider)
    - src/types/redux.ts (useAppSelector for Conflict)
    - vite.config.ts (confirm Vite resolves .tsx entry)
  </read_first>
  <action>
**Pre-conversion snapshot step:**
Capture snapshots of Conflict (with mocked globe children) and the root Provider/Router wrapper before converting.

1. **Conflict.jsx -> Conflict.tsx:**
   - Read the file carefully — this is a major container component that orchestrates the globe family
   - Convert class to functional
   - Replace `connect()` with `useAppSelector`/`useAppDispatch`
   - `this.state` -> useState hooks
   - Type all callbacks passed to child globe components
   - IMPORTANT: At this point, child globe components are still .jsx — TypeScript with `allowJs: true` resolves .jsx imports. The JSX children will be typed as `any` until Plan 07 converts them. This is acceptable.
   - Type the data state using WarEvent[] from types/api.ts

2. **index.jsx -> index.tsx:**
   - Delete `src/index.jsx`. Create `src/index.tsx`:
   ```typescript
   import React from 'react';
   import { createRoot } from 'react-dom/client';
   import { Provider } from 'react-redux';
   import store from './redux/store';

   import './styles/fonts.css';
   import './styles/reset.scss';

   import Router from './components/router/Router';

   const rootElement = document.getElementById('root');
   if (!rootElement) throw new Error('Root element not found');

   createRoot(rootElement).render(
     <Provider store={store}>
       <Router />
     </Provider>,
   );
   ```
   - The `if (!rootElement)` check satisfies strict null checks (getElementById returns HTMLElement | null)

3. Update `index.html` in project root — change the script src from `src/index.jsx` to `src/index.tsx` if present. Vite uses this as entry point.

4. **Post-conversion snapshot verification:** Re-run snapshot tests, diff against baseline.

5. Run full verification suite.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build && npm test</automated>
  </verify>
  <acceptance_criteria>
    - src/components/Conflict.jsx does NOT exist
    - src/components/Conflict.tsx contains "useAppSelector" or "useAppDispatch"
    - src/components/Conflict.tsx does NOT contain "extends React.Component"
    - src/components/Conflict.tsx does NOT contain "connect("
    - src/index.jsx does NOT exist
    - src/index.tsx contains "createRoot"
    - src/index.tsx contains "document.getElementById('root')"
    - index.html contains "src/index.tsx" (entry point updated)
    - React Test Renderer snapshots confirm no rendering regressions
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0
    - `npm test` exits 0
  </acceptance_criteria>
  <done>Conflict page converted to typed TSX with Redux hooks, app entry point converted to typed TSX with strict null check on root element, Vite entry updated, snapshot regression verified, build and tests pass</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — zero errors
- `npm run build` — Vite build succeeds
- `npm test` — all existing tests pass
- No .jsx files remain for: asylum components, RegionModal components, Conflict, index
- Zero instances of "extends React.Component" in converted files
- React Test Renderer snapshot diffs confirm zero rendering regressions
</verification>

<success_criteria>
All non-globe frontend components converted to TSX. Only the 6 globe components (GlobeVisual, GlobeContainer, GlobeTimeline, GlobeStatsBoard, GlobeTooltips, GlobeRouteButton) remain as .jsx files. Entry point is index.tsx. Snapshot regression tests pass. Build passes.
</success_criteria>

<output>
After completion, create `.planning/phases/07-start-v2-typescript-migration/07-06-SUMMARY.md`
</output>
