---
phase: 07-start-v2-typescript-migration
plan: 03
type: execute
wave: 1
depends_on: [07-01]
files_modified:
  - src/utils/api.ts
  - src/utils/color-conversion-algorithms.ts
  - src/components/utils/fetchers.ts
  - src/data/routeDictionary.ts
  - src/data/warDictionary.ts
  - src/THREEJSScript/EffectComposer.ts
  - src/THREEJSScript/Octree.ts
autonomous: true
requirements: [MOD-V2-01]

must_haves:
  truths:
    - "All utility and data dictionary files converted from .js to .ts with proper types"
    - "API fetch functions return typed Promises (not any)"
    - "Route and war dictionaries are typed const objects"
    - "Vendored THREE.js scripts converted to .ts per locked decision"
    - "tsc --noEmit passes with zero errors"
    - "Vite build succeeds"
  artifacts:
    - path: "src/utils/api.ts"
      provides: "Typed API fetch functions"
      exports: ["get_routeDeath", "get_routeCountryList", "get_routeIBC", "get_routeCrossingCount"]
    - path: "src/data/routeDictionary.ts"
      provides: "Typed route metadata"
      contains: "as const"
    - path: "src/data/warDictionary.ts"
      provides: "Typed war metadata"
      contains: "as const"
  key_links:
    - from: "src/utils/api.ts"
      to: "src/types/api.ts"
      via: "Response type imports"
      pattern: "import.*RouteDeath.*from.*types/api"
---

<objective>
Convert all utility functions, data dictionaries, and vendored THREE.js scripts from JavaScript to TypeScript.

Purpose: These files provide data shapes and helper functions consumed by every component. Converting them before components means component conversions will have typed imports available.
Output: 7 files converted from .js to .ts with proper types.
</objective>

<execution_context>
@/Users/abinabraham/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abinabraham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-start-v2-typescript-migration/07-CONTEXT.md
@.planning/phases/07-start-v2-typescript-migration/07-RESEARCH.md
@.planning/phases/07-start-v2-typescript-migration/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert API utilities and color conversion to TypeScript</name>
  <files>src/utils/api.ts, src/utils/color-conversion-algorithms.ts, src/components/utils/fetchers.ts</files>
  <read_first>
    - src/utils/api.js (current fetch wrappers with caching)
    - src/utils/color-conversion-algorithms.js (color conversion functions)
    - src/components/utils/fetchers.js (component-level fetch helpers)
    - src/types/api.ts (response types to import)
  </read_first>
  <action>
1. Delete `src/utils/api.js`. Create `src/utils/api.ts`:
   - Import response types from `../types/api` (RouteDeath, AsyApplication, IbcCrossing, CountryRoute, CrossingCountByCountry)
   - Type the cache variables: `let cached_routeDeath: Promise<RouteDeath[]> | null = null;` (and similarly for other caches)
   - Type each function's return value: `function get_routeDeath(): Promise<RouteDeath[]>`
   - Type `get_routeCountryList(): Promise<CountryRoute[]>`
   - Type `get_routeIBC(): Promise<IbcCrossing[]>`
   - Type `get_routeCrossingCount(): Promise<CrossingCountByCountry>`
   - The `crossingCountData` import from JSON stays — `resolveJsonModule: true` in tsconfig handles it
   - Keep `cashed_routeCrossingCount` variable name as-is (it's a misspelling in the original but changing it would break things)

2. Delete `src/utils/color-conversion-algorithms.js`. Create `src/utils/color-conversion-algorithms.ts`:
   - Read the file first to understand all exported functions
   - Add parameter and return types to each function (hslToRgb, rgbToHsl, etc.)
   - These are pure math functions — types are `number` inputs and outputs
   - Use tuple types where appropriate: `[number, number, number]` for RGB/HSL arrays

3. Delete `src/components/utils/fetchers.js`. Create `src/components/utils/fetchers.ts`:
   - Read the file first to understand exports
   - Add typed parameters and return types for all fetch helper functions
   - Import relevant types from `../../types/api` as needed
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/utils/api.js does NOT exist
    - src/utils/api.ts contains "Promise<RouteDeath[]>"
    - src/utils/api.ts contains "import" and "from '../types/api'"
    - src/utils/color-conversion-algorithms.js does NOT exist
    - src/utils/color-conversion-algorithms.ts exists
    - src/components/utils/fetchers.js does NOT exist
    - src/components/utils/fetchers.ts exists
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>All 3 utility files converted to TypeScript with typed function signatures and response types</done>
</task>

<task type="auto">
  <name>Task 2: Convert data dictionaries and vendored THREE.js scripts to TypeScript</name>
  <files>src/data/routeDictionary.ts, src/data/warDictionary.ts, src/THREEJSScript/EffectComposer.ts, src/THREEJSScript/Octree.ts</files>
  <read_first>
    - src/data/routeDictionary.js (route metadata object)
    - src/data/warDictionary.js (war metadata object)
    - src/THREEJSScript/EffectComposer.js (vendored THREE.js effect composer)
    - src/THREEJSScript/Octree.js (vendored THREE.js octree — may be disabled)
  </read_first>
  <action>
1. Delete `src/data/routeDictionary.js`. Create `src/data/routeDictionary.ts`:
   - Read current file to understand the exact export shape
   - Define an interface for the route entry (e.g. `RouteEntry` with fields like name, slug, description, etc.)
   - Export the dictionary with `as const` for literal string inference where helpful
   - Keep the same named export pattern as the original

2. Delete `src/data/warDictionary.js`. Create `src/data/warDictionary.ts`:
   - Read current file to understand the exact export shape
   - Define interface for war dictionary entries
   - Export with `as const` for literal inference
   - Keep the same named export pattern

3. Delete `src/THREEJSScript/EffectComposer.js`. Create `src/THREEJSScript/EffectComposer.ts`:
   - This is vendored THREE.js code — read it first to understand the class structure
   - Add type annotations for all class properties, method parameters, and return types
   - Import THREE types: `import * as THREE from 'three';`
   - For complex internal THREE.js types that don't have clean definitions, use `@ts-expect-error` with a comment explaining it's vendored code with internal THREE.js API usage
   - This is per the locked decision: vendored scripts converted to .ts, not left as .js with declarations

4. Delete `src/THREEJSScript/Octree.js`. Create `src/THREEJSScript/Octree.ts`:
   - Read the file first — note from RESEARCH.md: "Octree disabled — @brakebein/threeoctree useFaces on large merged BufferGeometry causes browser crash"
   - The code may be dead/disabled but must still be converted per locked decision
   - Add type annotations. For any THREE.js internal APIs that are hard to type, use `@ts-expect-error` with explanation
   - Import THREE types: `import * as THREE from 'three';`

5. Run `npx tsc --noEmit` to confirm zero errors.
6. Run `npm run build` to confirm Vite build.
7. Run `npm test` to confirm no regression.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build && npm test</automated>
  </verify>
  <acceptance_criteria>
    - src/data/routeDictionary.js does NOT exist
    - src/data/routeDictionary.ts exists and contains "export"
    - src/data/warDictionary.js does NOT exist
    - src/data/warDictionary.ts exists and contains "export"
    - src/THREEJSScript/EffectComposer.js does NOT exist
    - src/THREEJSScript/EffectComposer.ts exists and contains "import * as THREE"
    - src/THREEJSScript/Octree.js does NOT exist
    - src/THREEJSScript/Octree.ts exists
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0
    - `npm test` exits 0
  </acceptance_criteria>
  <done>All data dictionaries have typed interfaces, vendored THREE.js scripts converted to .ts, build and tests pass</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — zero errors
- `npm run build` — Vite build succeeds
- `npm test` — all existing tests pass
- No .js files remain in src/utils/, src/data/ (dictionaries), or src/THREEJSScript/
</verification>

<success_criteria>
All utility, data dictionary, and vendored script files are TypeScript with proper types. API functions return typed Promises. Data dictionaries use const assertions for literal inference. Component conversion plans can now import typed utilities.
</success_criteria>

<output>
After completion, create `.planning/phases/07-start-v2-typescript-migration/07-03-SUMMARY.md`
</output>
