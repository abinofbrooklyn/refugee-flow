---
phase: 07-start-v2-typescript-migration
plan: 09
type: execute
wave: 4
depends_on: [07-08]
files_modified:
  - server/ingestion/acledIngestion.ts
  - server/ingestion/alerter.ts
  - server/ingestion/cbpIngestion.ts
  - server/ingestion/countryNormalizer.ts
  - server/ingestion/eurostatIngestion.ts
  - server/ingestion/frontexIngestion.ts
  - server/ingestion/ingestionLogger.ts
  - server/ingestion/iomIngestion.ts
  - server/ingestion/iomNormalizer.ts
  - server/ingestion/quarterlyEstimator.ts
  - server/ingestion/retryRunner.ts
  - server/ingestion/ukChannelIngestion.ts
  - server/ingestion/unhcrIngestion.ts
  - server/ingestion/validator.ts
autonomous: true
requirements: [MOD-V2-01]

must_haves:
  truths:
    - "All 14 ingestion modules converted from .js to .ts"
    - "Ingestion functions return typed IngestionResult"
    - "Database queries use typed Knex generics from server/types/knex.ts"
    - "tsc --noEmit --project tsconfig.server.json passes"
    - "All 17 server tests pass (ingestion tests included)"
  artifacts:
    - path: "server/ingestion/acledIngestion.ts"
      provides: "Typed ACLED ingestion module"
      contains: "IngestionResult"
    - path: "server/ingestion/validator.ts"
      provides: "Typed data validator"
      contains: "export"
  key_links:
    - from: "server/ingestion/acledIngestion.ts"
      to: "server/types/knex.ts"
      via: "WarEventRow type for upsert"
      pattern: "WarEventRow"
    - from: "server/ingestion/ingestionLogger.ts"
      to: "server/types/ingestion.ts"
      via: "IngestionLogEntry type"
      pattern: "IngestionLogEntry"
---

<objective>
Convert all 14 server ingestion modules from JavaScript to TypeScript.

Purpose: The ingestion modules are the last server-side JavaScript files. They follow consistent patterns (fetch → normalize → upsert) making batch conversion efficient. The server/types/ from Plan 08 provide the row types these modules need.
Output: 14 ingestion files converted from .js to .ts.
</objective>

<execution_context>
@/Users/abinabraham/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abinabraham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-start-v2-typescript-migration/07-CONTEXT.md
@.planning/phases/07-start-v2-typescript-migration/07-RESEARCH.md
@.planning/phases/07-start-v2-typescript-migration/07-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert ingestion utility modules to TypeScript</name>
  <files>server/ingestion/alerter.ts, server/ingestion/countryNormalizer.ts, server/ingestion/ingestionLogger.ts, server/ingestion/iomNormalizer.ts, server/ingestion/quarterlyEstimator.ts, server/ingestion/retryRunner.ts, server/ingestion/validator.ts</files>
  <read_first>
    - server/ingestion/alerter.js (email alerting for failures)
    - server/ingestion/countryNormalizer.js (country name canonicalization)
    - server/ingestion/ingestionLogger.js (log to ingestion_log table)
    - server/ingestion/iomNormalizer.js (IOM data normalization)
    - server/ingestion/quarterlyEstimator.js (quarterly estimation from annual)
    - server/ingestion/retryRunner.js (retry logic for API calls)
    - server/ingestion/validator.js (data validation rules)
    - server/types/knex.ts (row types)
    - server/types/ingestion.ts (IngestionResult, IngestionLogEntry)
    - server/database/connection.ts (typed db)
  </read_first>
  <action>
For each of the 7 utility ingestion modules:

1. Read the current .js file completely
2. Delete the .js file
3. Create the .ts file with these transformations:
   - `const X = require('Y')` → `import X from 'Y'` or `import { X } from 'Y'`
   - `module.exports = { ... }` → named `export` for each function
   - Add parameter types and return types to all functions
   - Import row types from `../types/knex` where database queries are made
   - Import `IngestionResult`, `IngestionLogEntry` from `../types/ingestion` where applicable

**Specific notes per module:**

- **alerter.ts**: Uses `resend` for email — import Resend and type the email function parameters. If resend has built-in types, use them. Otherwise `@ts-expect-error` with note.

- **countryNormalizer.ts**: Pure function mapping country names. Type the lookup map as `Record<string, string>` and the function as `(name: string) => string`.

- **ingestionLogger.ts**: Writes to ingestion_log table. Type with `IngestionLogRow` from knex.ts. Function signature: `async function logIngestion(entry: IngestionLogEntry): Promise<void>`

- **iomNormalizer.ts**: Route normalization. Type the route mapping and normalization functions.

- **quarterlyEstimator.ts**: Distributes annual totals into quarters. Type the seasonal ratio data and estimation function.

- **retryRunner.ts**: Generic retry wrapper. Use TypeScript generics:
  ```typescript
  async function retryRunner<T>(
    fn: () => Promise<T>,
    maxRetries: number,
    delayMs: number
  ): Promise<T>
  ```

- **validator.ts**: Validation rules. Type the rule interface and validation result:
  ```typescript
  interface ValidationRule { ... }
  interface ValidationResult { clean: T[]; quarantined: T[]; }
  ```
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.server.json && npm test -- --testPathPattern="tests/server/(validator|retryRunner|alerter|iomNormalizer|asylumNormalizer)"</automated>
  </verify>
  <acceptance_criteria>
    - All 7 .js files in the list do NOT exist (deleted)
    - All 7 .ts files exist
    - None contain "module.exports" or "require(" (converted to import/export)
    - server/ingestion/retryRunner.ts contains generic type parameter (e.g., "<T>")
    - server/ingestion/ingestionLogger.ts contains "IngestionLogEntry" or "IngestionLogRow"
    - server/ingestion/validator.ts contains "export"
    - `npx tsc --noEmit --project tsconfig.server.json` exits 0
  </acceptance_criteria>
  <done>7 ingestion utility modules converted to typed TypeScript with proper imports, typed functions, and Knex row types</done>
</task>

<task type="auto">
  <name>Task 2: Convert ingestion pipeline modules to TypeScript</name>
  <files>server/ingestion/acledIngestion.ts, server/ingestion/cbpIngestion.ts, server/ingestion/eurostatIngestion.ts, server/ingestion/frontexIngestion.ts, server/ingestion/iomIngestion.ts, server/ingestion/ukChannelIngestion.ts, server/ingestion/unhcrIngestion.ts</files>
  <read_first>
    - server/ingestion/acledIngestion.js (ACLED API ingestion)
    - server/ingestion/cbpIngestion.js (CBP border crossing)
    - server/ingestion/eurostatIngestion.js (Eurostat asylum data)
    - server/ingestion/frontexIngestion.js (Frontex IBC data)
    - server/ingestion/iomIngestion.js (IOM Missing Migrants)
    - server/ingestion/ukChannelIngestion.js (UK small boat crossings)
    - server/ingestion/unhcrIngestion.js (UNHCR asylum data)
    - server/types/knex.ts (row types for upserts)
    - server/types/ingestion.ts (IngestionResult)
    - server/database/connection.ts (typed db)
  </read_first>
  <action>
For each of the 7 ingestion pipeline modules:

1. Read the current .js file completely
2. Delete the .js file
3. Create the .ts file with these transformations:
   - `const X = require('Y')` → `import X from 'Y'` or `import { X } from 'Y'`
   - `module.exports = { runXIngestion }` → `export { runXIngestion }` or `export async function runXIngestion(): Promise<IngestionResult>`
   - Type the main ingestion function to return `Promise<IngestionResult>` (from types/ingestion.ts)
   - Type all intermediate data shapes (API response parsing, CSV row shapes)
   - Type Knex queries with row types: `db<WarEventRow>('war_events').insert(...).onConflict('id').merge()`
   - Type fetch/HTTP call responses
   - For CSV parsing (csv-parse), type the parser options and row callback
   - For XML parsing (fast-xml-parser), type the parsed output shape

**Specific notes per module:**

- **acledIngestion.ts**: OAuth token fetch + paginated API. Type the ACLED API response shape and the OAuth token response. Uses `db<WarEventRow>` and `db<WarNoteRow>`.

- **cbpIngestion.ts**: Scrapes CBP data. Type the parsed data shape. Uses `db<IbcCrossingRow>`.

- **eurostatIngestion.ts**: Fetches Eurostat API JSON. Type the Eurostat response format.

- **frontexIngestion.ts**: IBC crossing data. Uses `db<IbcCrossingRow>`.

- **iomIngestion.ts**: Downloads CSV. Type CSV row shape. Uses `db<RouteDeathRow>`.

- **ukChannelIngestion.ts**: UK Home Office data. Type the parsed crossing data. Uses `db<IbcCrossingRow>`.

- **unhcrIngestion.ts**: UNHCR API with pagination. Type API response shape. Uses `db<AsyApplicationRow>`.

After all 7 are converted:
- Run `npx tsc --noEmit --project tsconfig.server.json`
- Run `npm test` (all 17 server tests)
  </action>
  <verify>
    <automated>npx tsc --noEmit --project tsconfig.server.json && npm test</automated>
  </verify>
  <acceptance_criteria>
    - All 7 .js ingestion pipeline files do NOT exist (deleted)
    - All 7 .ts files exist
    - None contain "module.exports" or "require(" (except dynamic require if needed)
    - server/ingestion/acledIngestion.ts contains "WarEventRow" or "WarNoteRow"
    - server/ingestion/iomIngestion.ts contains "RouteDeathRow"
    - server/ingestion/unhcrIngestion.ts contains "AsyApplicationRow"
    - Each module exports a `run*Ingestion` function returning `Promise<IngestionResult>`
    - No .js files remain in server/ingestion/ (all 14 converted)
    - `npx tsc --noEmit --project tsconfig.server.json` exits 0
    - `npm test` exits 0
  </acceptance_criteria>
  <done>All 14 server ingestion modules converted to TypeScript. Ingestion functions return typed IngestionResult. Knex queries use row type generics. Zero .js files remain in server/ingestion/. All tests pass.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit --project tsconfig.server.json` — zero errors
- `npm test` — all 17 server tests pass
- No .js files remain in server/ingestion/ (all 14 converted)
- `grep -r "module.exports" server/ingestion/` returns empty
- `grep -r "require(" server/ingestion/` returns empty (or only dynamic requires)
</verification>

<success_criteria>
All 14 ingestion modules are TypeScript with typed database queries, typed API responses, and typed function signatures. Zero .js files remain in the server/ directory (excluding node_modules). All server tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/07-start-v2-typescript-migration/07-09-SUMMARY.md`
</output>
