---
phase: 12-security-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .env.example
  - .gitignore
autonomous: true
requirements: []

must_haves:
  truths:
    - "helmet@^8.1.0 is installed, helmet@^3.18.0 is gone"
    - "@types/helmet is removed from devDependencies (v8 ships its own types)"
    - "multer is removed from dependencies (unused)"
    - "npm audit fix patches 6 of 7 fixable vulnerabilities"
    - ".env.supabase.bak is deleted from disk"
    - ".env.example documents RESEND_API_KEY and DATABASE_URL_PRODUCTION"
  artifacts:
    - path: "package.json"
      provides: "Updated deps: helmet v8, no multer, no @types/helmet"
      contains: '"helmet": "^8'
    - path: ".env.example"
      provides: "Template with all expected env vars documented"
      contains: "RESEND_API_KEY"
  key_links: []
---

<objective>
Upgrade Helmet to v8, remove unused dependencies (multer, @types/helmet), patch npm vulnerabilities, delete the .env.supabase.bak credential file from disk, and update .env.example to document all expected environment variables.

Purpose: Prepare the dependency and credential foundation so Plan 02 can configure Helmet v8, CSP, and the rest of the server hardening.
Output: Clean package.json with Helmet v8, no unused deps, patched vulns, no credential backup file on disk.
</objective>

<execution_context>
@/Users/abinabraham/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abinabraham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-security-hardening/12-CONTEXT.md
@.planning/phases/12-security-hardening/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade Helmet, remove unused deps, patch npm vulns</name>
  <files>package.json</files>
  <read_first>package.json</read_first>
  <action>
Run the following npm commands in sequence (all with --legacy-peer-deps per project convention due to eslint-config-airbnb peer conflict):

1. Upgrade Helmet from v3 to v8:
   ```
   npm install helmet@^8.1.0 --legacy-peer-deps
   ```

2. Remove @types/helmet (v8 ships bundled TypeScript types; the @types/helmet@^0.0.48 devDep is stale and will cause type conflicts):
   ```
   npm uninstall @types/helmet --legacy-peer-deps
   ```

3. Remove multer (installed but unused — the admin CSV upload uses a different approach):
   ```
   npm uninstall multer --legacy-peer-deps
   ```

4. Run npm audit fix to patch the 6 fixable vulnerabilities (handlebars critical, flatted high, path-to-regexp high, picomatch high, fast-xml-parser moderate, brace-expansion moderate):
   ```
   npm audit fix --legacy-peer-deps
   ```

5. Document the xlsx HIGH vulnerability as accepted risk. In package.json, update the `acceptedRisks` field:
   ```json
   "acceptedRisks": {
     "xlsx@0.18.5": "HIGH — Prototype Pollution and ReDoS. Used only in admin CSV upload flow behind ADMIN_SECRET. No upstream fix available."
   }
   ```

After all commands, verify:
- `npm ls helmet` shows 8.x
- `npm ls multer` shows not found
- `grep @types/helmet package.json` returns nothing
- `npm audit` shows only the xlsx advisory remaining
  </action>
  <verify>
    <automated>cd /Users/abinabraham/code/refugee-flow && npm ls helmet 2>&1 | grep -q "helmet@8" && echo "helmet v8 OK" && ! grep -q '"multer"' package.json && echo "multer removed OK" && ! grep -q '@types/helmet' package.json && echo "@types/helmet removed OK"</automated>
  </verify>
  <acceptance_criteria>
- grep '"helmet": "\\^8' package.json returns a match
- grep '"multer"' package.json returns NO match
- grep '@types/helmet' package.json returns NO match
- grep 'xlsx' package.json acceptedRisks section contains the documented risk
  </acceptance_criteria>
  <done>Helmet v8 installed, @types/helmet removed, multer removed, npm audit fix applied, xlsx risk documented in acceptedRisks</done>
</task>

<task type="auto">
  <name>Task 2: Delete credential backup and update .env.example</name>
  <files>.env.example, .gitignore</files>
  <read_first>.env.example, .gitignore</read_first>
  <action>
1. Delete the .env.supabase.bak file from disk (contains production Supabase and Resend credentials in plaintext):
   ```
   rm -f .env.supabase.bak
   ```
   NOTE: This file is NOT in git history (confirmed by research — `git log --all -- '.env.supabase.bak'` returns zero commits). The CONTEXT.md decision to run `git filter-repo` is a no-op because credentials were never committed. Only disk deletion is needed.

2. Update .env.example to document all expected environment variables. Replace the current content with:
   ```
   DATABASE_URL=postgresql://rfuser:rfpassword@localhost:5432/refugeeflow
   PORT=2700
   NODE_ENV=development

   # Production database (Supabase)
   # DATABASE_URL_PRODUCTION=postgresql://postgres.xxx:password@aws-0-us-east-1.pooler.supabase.com:6543/postgres

   # Data Ingestion API Keys (Phase 4)
   ACLED_EMAIL=your_acled_account_email
   ACLED_PASSWORD=your_acled_account_password

   # Admin shared secret (any long random string)
   ADMIN_SECRET=your_random_secret_here

   # Email alerts for ingestion failures (Resend)
   RESEND_API_KEY=your_resend_api_key_here
   ```

3. Verify .gitignore already covers both `.env` and `.env.supabase.bak`. Current .gitignore has both entries — no changes needed to .gitignore unless the entries are missing.

4. Add a checkpoint reminder to the user: After this plan completes, the user MUST:
   - Rotate the Supabase database password at supabase.com dashboard
   - Rotate the Resend API key at resend.com dashboard
   - Update their local .env with the new values
   This is a human-action item documented in the summary, NOT automated.
  </action>
  <verify>
    <automated>cd /Users/abinabraham/code/refugee-flow && test ! -f .env.supabase.bak && echo "bak deleted OK" && grep -q 'RESEND_API_KEY' .env.example && echo "env example OK" && grep -q 'DATABASE_URL_PRODUCTION' .env.example && echo "production URL documented OK"</automated>
  </verify>
  <acceptance_criteria>
- .env.supabase.bak does not exist on disk (test ! -f .env.supabase.bak succeeds)
- grep 'RESEND_API_KEY' .env.example returns a match
- grep 'DATABASE_URL_PRODUCTION' .env.example returns a match
- grep '.env.supabase.bak' .gitignore returns a match (already present)
  </acceptance_criteria>
  <done>.env.supabase.bak deleted from disk, .env.example updated with all expected vars including RESEND_API_KEY and DATABASE_URL_PRODUCTION, user reminded to rotate credentials</done>
</task>

</tasks>

<verification>
- `npm ls helmet` shows 8.x installed
- `npm audit` shows only xlsx remaining (HIGH, no fix available)
- `.env.supabase.bak` does not exist on disk
- `.env.example` contains all expected environment variable names
- `npm test -- --testPathPattern=tests/server --passWithNoTests` still passes (no regressions from dep changes)
</verification>

<success_criteria>
- Helmet v8 installed, multer and @types/helmet removed
- npm audit shows only the accepted xlsx vulnerability
- .env.supabase.bak deleted from disk
- .env.example documents DATABASE_URL_PRODUCTION and RESEND_API_KEY
- Existing tests pass without regression
</success_criteria>

<output>
After completion, create `.planning/phases/12-security-hardening/12-01-SUMMARY.md`
</output>
