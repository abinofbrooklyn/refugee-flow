---
phase: 02-modernize-stack
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/globe/GlobeVisual.jsx
  - src/components/globe/GlobeTooltips.jsx
  - src/components/globe/GlobeContainer.jsx
  - src/components/globe/GlobeRouteButton.jsx
  - src/components/globe/GlobeStatsBoard.jsx
  - src/components/RefugeeRoute_textArea.jsx
  - src/components/RefugeeRoute_textArea_content_currentSelectedPoint.jsx
  - src/components/RefugeeRoute_textArea_content_basicInfo.jsx
  - src/components/RefugeeRoute_map.jsx
  - src/components/RefugeeRoute_textArea_content_ibcCountry.jsx
  - src/components/RefugeeRoute_textArea_contentManager.jsx
  - src/components/RefugeeRoute_map_popup.jsx
  - src/components/asylumApplication/AsyApplicationChartContainer.jsx
  - src/components/RegionModalButton.jsx
  - src/components/RegionModalContent.jsx
  - src/components/RefugeeRoute_titleGroup.jsx
autonomous: true
requirements:
  - MOD-02

must_haves:
  truths:
    - "No componentWillReceiveProps deprecation warnings appear in the browser console"
    - "App renders and navigates between views with no behavior change"
  artifacts:
    - path: "src/components/globe/GlobeVisual.jsx"
      provides: "Globe rendering component with UNSAFE_ prefix"
      contains: "UNSAFE_componentWillReceiveProps"
    - path: "src/components/globe/GlobeContainer.jsx"
      provides: "Globe container with UNSAFE_ prefix"
      contains: "UNSAFE_componentWillReceiveProps"
  key_links:
    - from: "all 16 component files"
      to: "React 18 lifecycle API"
      via: "UNSAFE_componentWillReceiveProps rename"
      pattern: "UNSAFE_componentWillReceiveProps"
---

<objective>
Rename all `componentWillReceiveProps` methods to `UNSAFE_componentWillReceiveProps` across all 16 components that use this deprecated lifecycle method. This eliminates React 18 console deprecation warnings with zero behavior change.

Purpose: React 18 fires console warnings for unprefixed `componentWillReceiveProps`. The UNSAFE_ prefix suppresses warnings while preserving identical behavior. This is the user's preferred incremental approach — full refactor to `componentDidUpdate`/`getDerivedStateFromProps` is deferred to v2 (MOD-V2-01).

Output: 16 modified component files, zero console deprecation warnings.
</objective>

<execution_context>
@/Users/abinabraham/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abinabraham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-modernize-stack/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename componentWillReceiveProps to UNSAFE_ prefix in all 16 components</name>
  <files>
    src/components/globe/GlobeVisual.jsx
    src/components/globe/GlobeTooltips.jsx
    src/components/globe/GlobeContainer.jsx
    src/components/globe/GlobeRouteButton.jsx
    src/components/globe/GlobeStatsBoard.jsx
    src/components/RefugeeRoute_textArea.jsx
    src/components/RefugeeRoute_textArea_content_currentSelectedPoint.jsx
    src/components/RefugeeRoute_textArea_content_basicInfo.jsx
    src/components/RefugeeRoute_map.jsx
    src/components/RefugeeRoute_textArea_content_ibcCountry.jsx
    src/components/RefugeeRoute_textArea_contentManager.jsx
    src/components/RefugeeRoute_map_popup.jsx
    src/components/asylumApplication/AsyApplicationChartContainer.jsx
    src/components/RegionModalButton.jsx
    src/components/RegionModalContent.jsx
    src/components/RefugeeRoute_titleGroup.jsx
  </files>
  <action>
    In each of the 16 files listed above, find every occurrence of `componentWillReceiveProps` and rename it to `UNSAFE_componentWillReceiveProps`. This is a pure text replacement — the method signature, body, and all internal logic remain completely unchanged.

    Also check for any `.bind(this)` calls in constructors referencing `componentWillReceiveProps` — if any exist (unlikely for lifecycle methods, but check), update those references too.

    Do NOT refactor the method logic. Do NOT convert to componentDidUpdate or getDerivedStateFromProps. The UNSAFE_ prefix is intentional and supported by React 18.

    After renaming, verify no stale `componentWillReceiveProps` references remain anywhere in `src/`:
    `grep -r "componentWillReceiveProps" src/ --include="*.jsx" --include="*.js" | grep -v "UNSAFE_"`
    This should return zero results.
  </action>
  <verify>
    <automated>grep -r "componentWillReceiveProps" src/ --include="*.jsx" --include="*.js" | grep -v "UNSAFE_" | wc -l</automated>
  </verify>
  <done>All 16 files use UNSAFE_componentWillReceiveProps. grep returns 0 non-UNSAFE_ matches. No behavior change.</done>
</task>

<task type="auto">
  <name>Task 2: Verify app builds and existing tests pass</name>
  <files></files>
  <action>
    Run the existing build command to confirm the renamed lifecycle methods do not cause build failures:
    `NODE_OPTIONS=--openssl-legacy-provider npx webpack --env NODE_ENV=production --config webpack/webpack.prod.js`

    Then run the existing test suite:
    `npx jest`

    Both must succeed. The UNSAFE_ rename is a no-op at runtime, so no failures are expected.
  </action>
  <verify>
    <automated>npx jest 2>&1 | tail -5</automated>
  </verify>
  <done>Webpack build completes with exit 0. Jest test suite passes (same results as before the rename).</done>
</task>

</tasks>

<verification>
- `grep -r "componentWillReceiveProps" src/ --include="*.jsx" --include="*.js" | grep -v "UNSAFE_"` returns zero results
- `grep -rc "UNSAFE_componentWillReceiveProps" src/ --include="*.jsx" --include="*.js"` returns 16 files
- Webpack production build exits 0
- Jest passes
</verification>

<success_criteria>
All 16 components use UNSAFE_componentWillReceiveProps. Build and tests pass. Zero deprecated lifecycle warnings will fire in React 18 console.
</success_criteria>

<output>
After completion, create `.planning/phases/02-modernize-stack/02-01-SUMMARY.md`
</output>
