---
phase: 02-modernize-stack
plan: 04
type: execute
wave: 4
depends_on:
  - 02-03
files_modified:
  - package.json
  - src/components/RefugeeRoute_textArea.jsx
  - src/components/RefugeeRoute_textArea_content_basicInfo.jsx
  - src/components/RefugeeRoute_map.jsx
  - src/components/RefugeeRoute_textArea_content_ibcCountry.jsx
  - src/components/globe/GlobeContainer.jsx
  - src/components/asylumApplication/AsyApplicationChartContainer.jsx
  - src/components/RegionModalContent.jsx
  - src/components/RefugeeRoute_textArea_content_ibcCountryItem.jsx
  - src/components/Annotation.jsx
  - src/components/globe/GlobeVisual.jsx
  - src/components/globe/GlobeRouteButton.jsx
  - src/components/RefugeeRoute.jsx
  - src/components/globe/GlobeStatsBoard.jsx
  - src/components/RegionModalNav.jsx
  - src/components/RefugeeRoute_titleGroup.jsx
  - src/components/Conflict.jsx
  - src/components/RefugeeRoute_textArea_content_currentSelectedPoint.jsx
  - src/components/RefugeeRoute_textArea_contentManager.jsx
  - src/components/RefugeeRoute_map_popup.jsx
autonomous: true
requirements:
  - MOD-04

must_haves:
  truths:
    - "node_modules contains no jquery package"
    - "node_modules contains no underscore package"
    - "node_modules contains no mapbox-gl package"
    - "All components that used jquery now use native DOM APIs"
    - "All components that used underscore now use lodash"
  artifacts:
    - path: "package.json"
      provides: "Dependencies without jquery, underscore, or mapbox-gl"
  key_links:
    - from: "9 jquery-using components"
      to: "native DOM API"
      via: "el.offsetWidth, getBoundingClientRect, el.style, el.scrollTo"
      pattern: "offsetWidth|getBoundingClientRect|scrollTo"
    - from: "15 underscore-using components"
      to: "lodash"
      via: "import _ from 'lodash'"
      pattern: "from 'lodash'"
---

<objective>
Remove jquery, underscore, and mapbox-gl dependencies. Replace jquery DOM calls with native browser APIs, swap underscore imports to lodash (already installed), and replace the single mapbox-gl usage with maplibre-gl (already installed).

Purpose: These are dead-weight dependencies. jquery adds 87KB minified for trivial DOM reads. underscore duplicates lodash which is already in the bundle. mapbox-gl is partially migrated — one file still imports it.

Output: Three fewer runtime dependencies. All existing functionality preserved using built-in browser APIs and already-installed alternatives.
</objective>

<execution_context>
@/Users/abinabraham/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abinabraham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-modernize-stack/02-RESEARCH.md
@.planning/phases/02-modernize-stack/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace underscore with lodash in all 15 components</name>
  <files>
    src/components/globe/GlobeVisual.jsx
    src/components/RefugeeRoute_textArea.jsx
    src/components/RefugeeRoute_textArea_content_basicInfo.jsx
    src/components/RefugeeRoute_map.jsx
    src/components/RefugeeRoute_textArea_content_ibcCountry.jsx
    src/components/globe/GlobeContainer.jsx
    src/components/globe/GlobeRouteButton.jsx
    src/components/RefugeeRoute.jsx
    src/components/globe/GlobeStatsBoard.jsx
    src/components/RegionModalNav.jsx
    src/components/RegionModalContent.jsx
    src/components/RefugeeRoute_titleGroup.jsx
    src/components/RefugeeRoute_textArea_content_ibcCountryItem.jsx
    src/components/Conflict.jsx
    src/components/Annotation.jsx
  </files>
  <action>
    In each of the 15 files that import underscore, replace the import statement:
    - `import * as _ from 'underscore'` -> `import _ from 'lodash'`
    - `import _ from 'underscore'` -> `import _ from 'lodash'`
    - Any other underscore import variant -> `import _ from 'lodash'`

    All underscore methods used in this codebase (_.find, _.findIndex, _.groupBy, _.sortBy, _.reduce, _.uniq, _.debounce, _.delay, _.once, _.isMatch) have identical lodash equivalents. No call-site changes needed.

    After replacement, verify:
    `grep -r "from 'underscore'" src/ --include="*.jsx" --include="*.js"` returns zero results.
    `grep -r "require('underscore')" src/ --include="*.jsx" --include="*.js"` returns zero results.

    Then uninstall: `npm uninstall underscore`
  </action>
  <verify>
    <automated>grep -r "underscore" src/ --include="*.jsx" --include="*.js" | wc -l</automated>
  </verify>
  <done>All 15 files import from lodash instead of underscore. underscore removed from package.json. Zero underscore references in src/.</done>
</task>

<task type="auto">
  <name>Task 2: Replace jquery with native DOM APIs in 9 components, replace mapbox-gl with maplibre-gl in 1 component, uninstall both</name>
  <files>
    src/components/RefugeeRoute_textArea.jsx
    src/components/RefugeeRoute_textArea_content_basicInfo.jsx
    src/components/RefugeeRoute_map.jsx
    src/components/RefugeeRoute_textArea_content_ibcCountry.jsx
    src/components/globe/GlobeContainer.jsx
    src/components/asylumApplication/AsyApplicationChartContainer.jsx
    src/components/RegionModalContent.jsx
    src/components/RefugeeRoute_textArea_content_ibcCountryItem.jsx
    src/components/Annotation.jsx
    src/components/RefugeeRoute_textArea_content_currentSelectedPoint.jsx
    package.json
  </files>
  <action>
    **jQuery replacement (9 files):**

    Read each file and replace jQuery calls with native equivalents:

    | jQuery Pattern | Native Replacement |
    |---|---|
    | `$(el).width()` | `el.offsetWidth` or `el.getBoundingClientRect().width` |
    | `$(el).height()` | `el.offsetHeight` or `el.getBoundingClientRect().height` |
    | `$(el).offset()` | `el.getBoundingClientRect()` (returns {top, left, width, height}) |
    | `$('.class').css('prop', val)` | `document.querySelector('.class').style.prop = val` |
    | `$('#id').animate({scrollTop: n})` | `document.getElementById('id').scrollTo({top: n, behavior: 'smooth'})` |
    | `$('#id').scrollTop(0)` | `document.getElementById('id').scrollTop = 0` |
    | `$('.class')[0]` | `document.querySelector('.class')` |

    For `$.animate({scrollTop})`, use `el.scrollTo({top: value, behavior: 'smooth'})` — the `scroll-js` library is already a dependency if smoother animation is needed, but native `scrollTo` with `behavior: 'smooth'` is sufficient.

    After replacing all call sites, remove the jquery import from each file.

    **mapbox-gl replacement (1 file):**

    In `RefugeeRoute_textArea_content_currentSelectedPoint.jsx`:
    - Replace `import mapboxgl from 'mapbox-gl'` with `import maplibregl from 'maplibre-gl'`
    - Replace `mapboxgl.accessToken = '...'` — remove this line entirely (maplibre-gl doesn't need a token)
    - Replace `new mapboxgl.Map(...)` with `new maplibregl.Map(...)`
    - The Map constructor API is identical between mapbox-gl and maplibre-gl for the options this component uses

    **Uninstall:**
    ```
    npm uninstall jquery mapbox-gl
    ```

    **Verify no references remain:**
    `grep -r "from 'jquery'" src/` returns zero
    `grep -r "require('jquery')" src/` returns zero
    `grep -r "from 'mapbox-gl'" src/` returns zero

    **Verify build:**
    `npx vite build` exits 0
  </action>
  <verify>
    <automated>npx vite build 2>&1 | tail -5 && echo "---" && node -e "try{require('jquery');console.log('FAIL: jquery found')}catch(e){console.log('OK: jquery removed')}" && node -e "try{require('mapbox-gl');console.log('FAIL: mapbox-gl found')}catch(e){console.log('OK: mapbox-gl removed')}"</automated>
  </verify>
  <done>jquery and mapbox-gl uninstalled. All 9 jquery-using files use native DOM APIs. The mapbox-gl file uses maplibre-gl. Vite build passes. No jquery/mapbox-gl references in src/.</done>
</task>

</tasks>

<verification>
- `npx vite build` exits 0
- `node -e "require('jquery')"` throws (not installed)
- `node -e "require('underscore')"` throws (not installed)
- `node -e "require('mapbox-gl')"` throws (not installed)
- `grep -r "from 'jquery'" src/` returns nothing
- `grep -r "from 'underscore'" src/` returns nothing
- `grep -r "from 'mapbox-gl'" src/` returns nothing
- `npx jest` passes
</verification>

<success_criteria>
jquery, underscore, and mapbox-gl are not in package.json or node_modules. All component functionality preserved using lodash, native DOM APIs, and maplibre-gl. Build passes. Tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/02-modernize-stack/02-04-SUMMARY.md`
</output>
