---
phase: 01-stabilize
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/globe/GlobeContainer.jsx
autonomous: true
requirements:
  - STAB-04
  - STAB-05
must_haves:
  truths:
    - "A visible UI control lets users toggle globe auto-rotation on and off"
    - "npm audit reports zero critical or high severity vulnerabilities after patches applied"
  artifacts:
    - path: "src/components/globe/GlobeContainer.jsx"
      provides: "Globe rotation toggle button in render()"
      contains: "rotatePause, onClick, setState"
  key_links:
    - from: "GlobeContainer rotation toggle button"
      to: "GlobeVisual.componentWillReceiveProps"
      via: "rotatePause prop passed from GlobeContainer state"
      pattern: "rotatePause.*this\\.state\\.rotatePause"
---

<objective>
Add a globe rotation toggle button to GlobeContainer and patch all known security vulnerabilities in the dependency tree.

Purpose: Users have no way to stop the auto-rotating globe (disorienting for keyboard navigation and careful inspection). The dependency tree has critical and high CVEs that need to be resolved before Phase 2 stack changes.

Output: One modified UI file (globe toggle button) and a clean npm audit. These two concerns are combined because both are small, independent, and touch different parts of the system.
</objective>

<execution_context>
@/Users/abinabraham/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abinabraham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-stabilize/01-CONTEXT.md
@.planning/phases/01-stabilize/01-RESEARCH.md

<interfaces>
<!-- Key wiring already in place (from research). -->

GlobeContainer.jsx rotation wiring (fully implemented — only the button is missing):
1. GlobeContainer.state.rotatePause = false (default, line ~327)
2. <GlobeVisual rotatePause={this.state.rotatePause} .../> (line ~569)
3. GlobeVisual.componentWillReceiveProps receives rotatePause and sets this.rotatePause (line ~607)
4. animate() calls rotateGlobe(2/1000, this.rotatePause) (line ~879)
5. rotateGlobe skips rotation when cancel === true (lines ~632-640)

The toggle button only needs to call:
  this.setState(prev => ({ rotatePause: !prev.rotatePause }))

Style reference: GlobeControllerButton styled-component already defined in GlobeContainer.jsx (lines ~98-132). Use the same pattern for the toggle button so it matches the existing controls visually.

Button icon guidance (Claude's discretion — play/pause is appropriate):
  {this.state.rotatePause ? '▶' : '⏸'}
  aria-label: this.state.rotatePause ? 'Resume globe rotation' : 'Pause globe rotation'

Security patch approach:
  Step 1: npm audit fix        (resolves critical + most high without breaking changes)
  Step 2: npm audit fix --force (upgrades webpack-dev-server to v5 to resolve ansi-html HIGH)
  Step 3: npm audit            (confirm zero critical/high remain)
  Step 4: npm start            (confirm dev server still works after --force upgrade)
  If webpack-dev-server@5 breaks the dev config, check webpack/webpack.dev.js for incompatible devServer options (WDS 5 changed config keys). Document any config fixes needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add globe rotation toggle button to GlobeContainer</name>
  <files>src/components/globe/GlobeContainer.jsx</files>
  <action>
    Read GlobeContainer.jsx first to locate the render() method and the existing globe controls area.

    Find where the existing globe control buttons are rendered (look for GlobeControllerButton or similar styled components near the globe area in render).

    Add a rotation toggle button in that same area:

    ```javascript
    <button
      onClick={() => this.setState(prev => ({ rotatePause: !prev.rotatePause }))}
      aria-label={this.state.rotatePause ? 'Resume globe rotation' : 'Pause globe rotation'}
      title={this.state.rotatePause ? 'Resume rotation' : 'Pause rotation'}
    >
      {this.state.rotatePause ? '▶' : '⏸'}
    </button>
    ```

    Style it using the existing GlobeControllerButton styled-component (or whichever styled button component is already used for the globe controls). Do not add new styled-component definitions unless nothing appropriate exists.

    Position: place it naturally near the other globe control buttons (zoom, reset, etc.) so users find it without instruction.

    Verify the rotatePause state is already initialized in the constructor/state (line ~327 per research) — do not add a duplicate state field.

    Do NOT modify the rotatePause prop flow — that wiring is already complete and working.
  </action>
  <verify>
    <automated>npm test -- --passWithNoTests 2>&1 | tail -5</automated>
    Manual: Start dev server, open globe view, click toggle button — globe stops rotating. Click again — globe resumes. Button label/icon changes with state.
  </verify>
  <done>
    GlobeContainer.jsx has a toggle button that: calls setState to flip rotatePause, displays play/pause icon reflecting current state, is positioned with the existing globe controls, and uses existing styled-component styling. No new imports or state fields added.
  </done>
</task>

<task type="auto">
  <name>Task 2: Patch security vulnerabilities with npm audit fix</name>
  <files>package.json, package-lock.json</files>
  <action>
    Run the following in sequence:

    ```bash
    npm audit fix
    npm audit fix --force
    npm audit
    ```

    After npm audit, check the output:
    - If zero critical and zero high: task complete.
    - If vulnerabilities remain that have no available fix (no patch exists in the npm registry), document each one with its package name, severity, and reason no fix is available. Accept as known risk for packages being removed in Phase 2 (e.g., mapbox-gl is a Phase 2 removal candidate).
    - If npm audit fix --force introduced breaking changes to webpack-dev-server config, run `npm start` to test. If it fails, inspect webpack/webpack.dev.js for options that changed in webpack-dev-server v5 (common: devServer.contentBase → devServer.static, devServer.hot, etc.) and fix only what is needed to restore dev server startup. Do NOT upgrade webpack itself or change other dependencies.

    Run `npm start` after patching to confirm the dev server still starts.

    Do not pin specific versions unnecessarily. Let npm audit fix choose the safe upgrade paths.
  </action>
  <verify>
    <automated>npm audit --audit-level=high 2>&1; echo "Exit: $?"</automated>
  </verify>
  <done>
    npm audit reports zero critical and zero high severity vulnerabilities. If any unfixable issues remain, they are documented in the summary with justification (e.g., "ansi-html in legacy webpack-dev-server: no patch available without removing mapbox-gl, deferred to Phase 2 cleanup"). Dev server starts successfully.
  </done>
</task>

</tasks>

<verification>
1. `npm audit --audit-level=high` exits with code 0 (zero critical/high)
2. `npm test -- --passWithNoTests` exits clean
3. Globe rotation toggle button visible and functional in dev server
4. Dev server starts (`npm start`) without errors after patches
</verification>

<success_criteria>
- GlobeContainer.jsx has a rotation toggle button wired to this.state.rotatePause via setState
- Button shows play/pause icon that reflects current state
- npm audit reports zero critical or high severity vulnerabilities (or any remaining are documented as accepted risk with justification)
- Dev server still starts after npm audit fix --force
</success_criteria>

<output>
After completion, create `.planning/phases/01-stabilize/01-03-SUMMARY.md` documenting:
- Rotation toggle: exact position in render(), styled-component used
- Security patches: packages upgraded, final npm audit output (critical/high counts before and after)
- Any webpack-dev-server config changes required after --force upgrade
- Any accepted-risk vulnerabilities with justification
</output>
