---
phase: 01-stabilize
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/globe/GlobeContainer.jsx
  - src/components/RefugeeRoute.jsx
  - src/components/globe/GlobeRouteButton.jsx
  - src/utils/api.js
  - src/components/utils/fetchers.js
autonomous: false
requirements:
  - STAB-02
  - STAB-03
must_haves:
  truths:
    - "User sees a spinner while any data fetch is in progress"
    - "User sees a clear error message if any data fetch fails — no blank or frozen view"
    - "GlobeContainer loading spinner hides if the API call fails (does not hang forever)"
  artifacts:
    - path: "src/components/globe/GlobeContainer.jsx"
      provides: "Loading + error state for globe data fetch"
      contains: "loadingError, catch"
    - path: "src/components/RefugeeRoute.jsx"
      provides: "Loading + error state for route fetches"
      contains: "loading, error, ScaleLoader"
    - path: "src/components/globe/GlobeRouteButton.jsx"
      provides: "Error state for country list fetch"
      contains: "error, catch"
    - path: "src/utils/api.js"
      provides: "Error propagation from fetch wrappers"
      contains: "catch"
    - path: "src/components/utils/fetchers.js"
      provides: "Error propagation from fetcher helpers"
      contains: "catch"
  key_links:
    - from: "GlobeContainer.fetchData()"
      to: "loadingStatus state"
      via: ".catch() handler sets loadingStatus false"
      pattern: "loadingStatus.*false"
    - from: "RefugeeRoute.fetchRefugeeRoutes()"
      to: "error state"
      via: ".catch() on both get_routeDeath and get_routeIBC"
      pattern: "setState.*error"
---

<objective>
Add loading indicators and error messages to every component that fetches data, so users always know the app's state and never see a blank or frozen view.

Purpose: Currently all data fetches silently swallow errors and leave loading spinners spinning forever if the API is down. This makes failures invisible and the app appear broken.

Output: Five modified files — no new dependencies needed (react-spinners and LoadingBar.jsx are already in the codebase and available to import).
</objective>

<execution_context>
@/Users/abinabraham/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abinabraham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-stabilize/01-CONTEXT.md
@.planning/phases/01-stabilize/01-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from research. -->

Existing loading infrastructure (already installed, no new deps needed):
- ScaleLoader from 'react-spinners' — already imported in GlobeContainer and AsyApplicationContainer
- src/components/LoadingBar.jsx exports: LoadingDivWrapper, LoaderGraphWrapper, LoadingIndicator

Fetch points that need error handling (from research):

1. GlobeContainer.jsx — fetchData() in componentDidMount:
   - Has .then().then().then() chain with no .catch()
   - Uses this.setState({ loadingStatus: false }) on success — add matching setState on error
   - Add: .catch(err => this.setState({ loadingStatus: false, loadingError: err.message }))
   - In render(): if this.state.loadingError, show error message (inline <p> or styled div)

2. RefugeeRoute.jsx — fetchRefugeeRoutes():
   - Calls get_routeDeath() and get_routeIBC() from api.js
   - Neither has .catch()
   - Add loading and error to state: { loading: true, error: null }
   - Set loading: true before fetch; loading: false on both success and catch
   - If error: render <p>Failed to load route data. Please refresh.</p> using existing styled components

3. GlobeRouteButton.jsx — calls get_routeCountryList() from api.js:
   - No .catch() on the fetch
   - Add error state and display inline error message on failure

4. src/utils/api.js — get_routeDeath, get_routeIBC, get_routeCountryList:
   - All use fetch(request).then(res => res.json()) with no .catch()
   - Add .catch(err => { throw err; }) to propagate errors to callers
   - Do NOT swallow errors in the utility — let callers handle them

5. src/components/utils/fetchers.js — fetcher helpers called by AsyApplicationContainer:
   - Add .catch() propagation so callers can handle errors

Spinner style guidance (Claude's discretion — centered overlay preferred):
- Use ScaleLoader for full-page data loads (consistent with GlobeContainer existing pattern)
- Place inline error message below where the component's main content would render
- Error message text: 'Failed to load data. Please refresh.' (no retry button per locked decision)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add error propagation to api.js and fetchers.js</name>
  <files>src/utils/api.js, src/components/utils/fetchers.js</files>
  <action>
    Read both files first before making any changes.

    src/utils/api.js:
    - For each exported function that calls fetch() (get_routeDeath, get_routeIBC, get_routeCountryList, and any others):
      - Add .catch(err => { throw err; }) at the end of the promise chain so errors propagate to the caller
      - Do NOT swallow errors or return default values — the caller must see the failure

    src/components/utils/fetchers.js:
    - Apply the same pattern: add .catch(err => { throw err; }) to each fetch chain
    - Do NOT add UI logic here — just propagate

    The goal of this task is to make errors visible to callers. Task 2 handles the UI responses.
  </action>
  <verify>
    <automated>npm test -- --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>
    api.js and fetchers.js throw errors on fetch failure instead of silently returning undefined or stalling. Every exported fetch function has a .catch(err => { throw err; }) or equivalent propagation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add loading and error states to GlobeContainer, RefugeeRoute, and GlobeRouteButton</name>
  <files>src/components/globe/GlobeContainer.jsx, src/components/RefugeeRoute.jsx, src/components/globe/GlobeRouteButton.jsx</files>
  <action>
    Read all three files first before making changes.

    GlobeContainer.jsx:
    - The component already has loadingStatus in state (default true). Add loadingError: null to initial state.
    - Find the fetchData() promise chain in componentDidMount (the .then().then().then() chain with no catch).
    - Add at the end: .catch(err => this.setState({ loadingStatus: false, loadingError: err.message || 'Failed to load data. Please refresh.' }))
    - In render(): if this.state.loadingError is truthy, render an error message. Place it where the loading spinner currently renders (or alongside it). Use a simple styled div or <p> — do not add a new styled-component library. Match the visual style of adjacent elements (dark background, light text is likely appropriate given globe theme).
    - Keep the existing spinner render for when loadingStatus is true and loadingError is null.

    RefugeeRoute.jsx:
    - Add loading: true and error: null to component state (or constructor).
    - In fetchRefugeeRoutes() (or componentDidMount), set loading: true before the fetch calls.
    - Both get_routeDeath() and get_routeIBC() are called — each needs its own .catch() handler, or wrap both in Promise.all() with a single catch. Either approach is fine; choose the cleaner option given the existing code structure.
    - On success: setState({ loading: false }).
    - On error: setState({ loading: false, error: 'Failed to load route data. Please refresh.' }).
    - In render(): if loading is true, return <ScaleLoader /> (import from 'react-spinners') or use LoadingDivWrapper/LoadingIndicator from src/components/LoadingBar.jsx. If error is truthy, return a simple error message element. Style to match the component's existing visual context.

    GlobeRouteButton.jsx:
    - Add error: null to component state.
    - Find the get_routeCountryList() call — add .catch(err => this.setState({ error: 'Failed to load route data.' })).
    - In render(): if error is truthy, show the error message inline (where the button or route list would normally render).
    - Keep existing loading state if one exists; add it if it doesn't.

    Style guidance (per Claude's discretion):
    - Error message: red or amber text, same font as surrounding elements, no icons required
    - Do NOT use toast libraries (not installed; inline per locked decision)
    - Do NOT add a retry button (explicitly out of scope per locked decision)
  </action>
  <verify>
    <automated>npm test -- --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>
    - GlobeContainer.jsx: fetchData() has .catch() that sets loadingStatus false and shows error message in render
    - RefugeeRoute.jsx: has loading and error state; spinner shows during fetch; error message shows on failure
    - GlobeRouteButton.jsx: has error state; error message shows on fetch failure
    - No new npm dependencies added
    - No retry button (per locked decision)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify loading and error states in browser</name>
  <action>
    Pause for human verification of loading spinners and error messages in the running application.
    Tasks 1 and 2 have completed all automated implementation. Human must verify the visual behavior.
  </action>
  <verify>
    <automated>npm test -- --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>Human confirms spinner visible during fetch and error message visible when backend is unavailable.</done>
  <what-built>
    Loading spinners and error messages added to all data-fetching components. Error propagation added to api.js and fetchers.js. No new dependencies.
  </what-built>
  <how-to-verify>
    Test loading state:
    1. Start the app with the backend running: `npm start` in one terminal, `npm run server` in another (or however the dev server is started)
    2. Open the app in Chrome DevTools → Network tab → set throttle to "Slow 3G"
    3. Navigate to the globe view — a spinner should appear while data loads
    4. Navigate to the route view — a spinner should appear there too

    Test error state:
    5. Stop the backend server
    6. Reload the app in the browser
    7. Confirm: a clear error message appears (not a blank view, not a frozen spinner)
    8. Check GlobeContainer, RefugeeRoute view, and GlobeRouteButton if accessible

    Expected outcome: spinner visible during slow load, error message visible when backend is down.
  </how-to-verify>
  <resume-signal>Type "approved" if loading and error states work correctly, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. `npm test -- --passWithNoTests` exits clean
2. grep for `.catch` in api.js, fetchers.js, GlobeContainer.jsx, RefugeeRoute.jsx, GlobeRouteButton.jsx — each file must have at least one
3. grep for `loadingError` in GlobeContainer.jsx — must exist in state and render
4. Human checkpoint approved (loading/error states visible in browser)
</verification>

<success_criteria>
- Every data fetch in the app has a .catch() handler
- Spinner visible during fetch in globe view and route view
- Error message visible (not blank/frozen) when backend is unavailable
- No new npm packages installed
- No retry button added (per locked decision)
- GlobeContainer loading spinner does not hang forever on API failure
</success_criteria>

<output>
After completion, create `.planning/phases/01-stabilize/01-02-SUMMARY.md` documenting:
- Which files were modified
- The error state field names added to each component
- Spinner component used (ScaleLoader vs LoadingBar)
- Error message text and placement decisions (per Claude's discretion)
- Result of human checkpoint
</output>
